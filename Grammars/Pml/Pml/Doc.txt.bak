The Language Pml
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Pml==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




PIdent literals are recognized by the regular expression
`````('_' | letter) ('_' | digit | letter)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Pml are the following:
  | ``active`` | ``assert`` | ``atomic`` | ``bit``
  | ``bool`` | ``break`` | ``byte`` | ``chan``
  | ``d_step`` | ``do`` | ``else`` | ``empty``
  | ``enabled`` | ``eval`` | ``false`` | ``fi``
  | ``for`` | ``full`` | ``get_priority`` | ``goto``
  | ``hidden`` | ``if`` | ``in`` | ``init``
  | ``inline`` | ``int`` | ``len`` | ``mtype``
  | ``nempty`` | ``never`` | ``nfull`` | ``np_``
  | ``od`` | ``of`` | ``pc_value`` | ``print``
  | ``printf`` | ``printm`` | ``priority`` | ``proctype``
  | ``provided`` | ``run`` | ``select`` | ``set_priority``
  | ``short`` | ``show`` | ``skip`` | ``timeout``
  | ``trace`` | ``true`` | ``typedef`` | ``unless``
  | ``unsigned`` | ``xr`` | ``xs`` |

The symbols used in Pml are the following:
  | ; | -> | && | ||
  | + | - | * | /
  | % | & | ^ | |
  | > | < | >= | <=
  | == | != | << | >>
  | ~ | ! | ( | )
  | { | } | = | ,
  | : | [ | ] | @
  | . | ? | .. | ::

===Comments===
There are no single-line comments in the grammar.There are no multiple-line comments in the grammar.

==The syntactic structure of Pml==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //[Module]// | -> | //Module//
  |  |  **|**  | //Module// //[Module]//
  | //Visible// | -> | ``hidden``
  |  |  **|**  | ``show``
  | //ChanPoll// | -> | ``full``
  |  |  **|**  | ``empty`` ``nfull`` ``nempty``
  | //Separator// | -> | ``;``
  |  |  **|**  | ``->``
  | //Typename// | -> | ``bit``
  |  |  **|**  | ``bool``
  |  |  **|**  | ``byte``
  |  |  **|**  | ``short``
  |  |  **|**  | ``int``
  |  |  **|**  | ``mtype``
  |  |  **|**  | ``chan``
  |  |  **|**  | //Uname//
  | //AndOr// | -> | ``&&``
  |  |  **|**  | ``||``
  | //BinOp// | -> | ``+``
  |  |  **|**  | ``-``
  |  |  **|**  | ``*``
  |  |  **|**  | ``/``
  |  |  **|**  | ``%``
  |  |  **|**  | ``&``
  |  |  **|**  | ``^``
  |  |  **|**  | ``|``
  |  |  **|**  | ``>``
  |  |  **|**  | ``<``
  |  |  **|**  | ``>=``
  |  |  **|**  | ``<=``
  |  |  **|**  | ``==``
  |  |  **|**  | ``!=``
  |  |  **|**  | ``<<``
  |  |  **|**  | ``>>``
  |  |  **|**  | //AndOr//
  | //UnrOp// | -> | ``~``
  |  |  **|**  | ``-``
  |  |  **|**  | ``!``
  | //Const// | -> | ``true``
  |  |  **|**  | ``false``
  |  |  **|**  | ``skip``
  |  |  **|**  | //Integer//
  | //PrintType// | -> | ``print``
  |  |  **|**  | ``printf``
  |  |  **|**  | ``printm``
  | //Module// | -> | //Proctype//
  |  |  **|**  | //Inline//
  |  |  **|**  | //Init//
  |  |  **|**  | //Never//
  |  |  **|**  | //Trace//
  |  |  **|**  | //Utype//
  |  |  **|**  | //Mtype//
  |  |  **|**  | //DeclList//
  | //Proctype// | -> | //Pactive// ``proctype`` //Name// ``(`` //PdeclList// ``)`` //Ppriority// //Penabler// ``{`` //Sequence// ``}``
  | //Inline// | -> | ``inline`` //Name// ``(`` //ArgList// ``)`` ``{`` //Sequence// ``}``
  | //Pactive// | -> | **eps**
  |  |  **|**  | //Active//
  | //PdeclList// | -> | **eps**
  |  |  **|**  | //DeclList//
  | //Ppriority// | -> | **eps**
  |  |  **|**  | //Priority//
  | //Penabler// | -> | **eps**
  |  |  **|**  | //Enabler//
  | //Init// | -> | ``init`` //Ipriority// ``{`` //Sequence// ``}``
  | //Ipriority// | -> | **eps**
  |  |  **|**  | //Priority//
  | //Never// | -> | ``never`` ``{`` //Sequence// ``}``
  | //Trace// | -> | ``trace`` ``{`` //Sequence// ``}``
  | //Utype// | -> | ``typedef`` //Name// ``{`` //DeclList// ``}`` ``;``
  | //Mtype// | -> | ``mtype`` //Mequals// ``{`` //Mname// ``}`` //Msep//
  | //Msep// | -> | **eps**
  |  |  **|**  | ``;``
  | //Mequals// | -> | **eps**
  |  |  **|**  | ``=``
  | //Mname// | -> | //Name//
  |  |  **|**  | //Name// ``,`` //Mname//
  | //DeclList// | -> | //Decl// //Separator//
  |  |  **|**  | //Decl//
  |  |  **|**  | //Decl// //Separator// //DeclList//
  | //Decl// | -> | //DeclVisible// //Typename// //DclIvar//
  |  |  **|**  | //DeclVisible// //UnsignedDecl//
  | //DclIvar// | -> | //Ivar// ``,`` //DclIvar//
  |  |  **|**  | //Ivar//
  | //DeclVisible// | -> | **eps**
  |  |  **|**  | //Visible//
  | //UnsignedDecl// | -> | ``unsigned`` //Name// ``:`` //Const// //UDclAssign//
  | //UDclAssign// | -> | **eps**
  |  |  **|**  | ``=`` //AnyExpr//
  | //Active// | -> | ``active`` //AConst//
  | //AConst// | -> | **eps**
  |  |  **|**  | ``[`` //Const// ``]``
  | //Priority// | -> | ``priority`` //Const//
  | //Enabler// | -> | ``provided`` ``(`` //Expr// ``)``
  | //Sequence// | -> | //Step//
  |  |  **|**  | //Step// //Separator//
  |  |  **|**  | //Step// //Sequence//
  |  |  **|**  | //Step// //Separator// //Sequence//
  | //UStmt// | -> | **eps**
  |  |  **|**  | ``unless`` //Stmt//
  | //Step// | -> | //Mtype//
  |  |  **|**  | //Stmt// //UStmt//
  |  |  **|**  | //DeclList//
  |  |  **|**  | ``xr`` //VarRefList//
  |  |  **|**  | ``xs`` //VarRefList//
  | //VarRefList// | -> | //VarRef//
  |  |  **|**  | //VarRef// ``,`` //VarRefList//
  | //AnyExpr// | -> | ``(`` //AnyExpr// ``)``
  |  |  **|**  | //AnyExpr// //BinOp// //AnyExpr//
  |  |  **|**  | //UnrOp// //AnyExpr//
  |  |  **|**  | ``(`` //AnyExpr// ``->`` //AnyExpr// ``:`` //AnyExpr// ``)``
  |  |  **|**  | ``len`` ``(`` //VarRef// ``)``
  |  |  **|**  | //Poll//
  |  |  **|**  | //VarRef//
  |  |  **|**  | //Const//
  |  |  **|**  | ``timeout``
  |  |  **|**  | ``np_``
  |  |  **|**  | ``enabled`` ``(`` //AnyExpr// ``)``
  |  |  **|**  | ``pc_value`` ``(`` //AnyExpr// ``)``
  |  |  **|**  | //Name// ``[`` //AnyExpr// ``]`` ``@`` //Name//
  |  |  **|**  | ``run`` //Name// ``(`` //RunArgs// ``)`` //RunPrio//
  |  |  **|**  | ``get_priority`` ``(`` //Expr// ``)``
  |  |  **|**  | ``set_priority`` ``(`` //Expr// ``,`` //Expr// ``)``
  | //Ivar// | -> | //Name// //IvarConst// //IvarAssign//
  | //IvarConst// | -> | **eps**
  |  |  **|**  | ``[`` //Const// ``]``
  | //IvarAssign// | -> | **eps**
  |  |  **|**  | ``=`` //AnyExpr//
  |  |  **|**  | ``=`` //ChInit//
  | //ChInit// | -> | ``[`` //Const// ``]`` ``of`` ``{`` //ChType// ``}``
  | //ChType// | -> | //Typename//
  |  |  **|**  | //Typename// ``,`` //ChType//
  | //VarRef// | -> | //Name// //VarRefAnyExpr// //VarRefTypedef//
  | //VarRefAnyExpr// | -> | **eps**
  |  |  **|**  | ``[`` //AnyExpr// ``]``
  | //VarRefTypedef// | -> | **eps**
  |  |  **|**  | ``.`` //VarRef//
  | //Send// | -> | //VarRef// ``!`` //SendArgs//
  |  |  **|**  | //VarRef// ``!`` ``!`` //SendArgs//
  | //Receive// | -> | //VarRef// ``?`` //RecvArgs//
  |  |  **|**  | //VarRef// ``?`` ``?`` //RecvArgs//
  |  |  **|**  | //VarRef// ``?`` ``<`` //RecvArgs// ``>``
  |  |  **|**  | //VarRef// ``?`` ``?`` ``<`` //RecvArgs// ``>``
  | //Poll// | -> | //VarRef// ``?`` ``[`` //RecvArgs// ``]``
  |  |  **|**  | //VarRef// ``?`` ``?`` ``[`` //RecvArgs// ``]``
  | //SendArgs// | -> | //ArgList//
  |  |  **|**  | //AnyExpr// ``(`` //ArgList// ``)``
  | //ArgList// | -> | //AnyExpr// ``,`` //ArgList//
  |  |  **|**  | //AnyExpr//
  |  |  **|**  | **eps**
  | //RecvArgs// | -> | //RecvArgList//
  |  |  **|**  | //RecvArgList// ``(`` //RecvArgs// ``)``
  | //RecvArgList// | -> | //RecvArg//
  |  |  **|**  | //RecvArg// //RecvArgList//
  | //UnaryMinus// | -> | **eps**
  |  |  **|**  | ``-``
  | //RecvArg// | -> | //VarRef//
  |  |  **|**  | ``eval`` ``(`` //VarRef// ``)``
  |  |  **|**  | //UnaryMinus// //Const//
  | //Assign// | -> | //VarRef// ``=`` //AnyExpr//
  |  |  **|**  | //VarRef// ``+`` ``+``
  |  |  **|**  | //VarRef// ``-`` ``-``
  | //Pargs// | -> | //String//
  |  |  **|**  | //ArgList//
  |  |  **|**  | //String// ``,`` //ArgList//
  | //PArgList// | -> | **eps**
  | //PargList// | -> | ``,`` //ArgList//
  | //Stmt// | -> | ``if`` //Options// ``fi``
  |  |  **|**  | ``do`` //Options// ``od``
  |  |  **|**  | ``for`` ``(`` //Range// ``)`` ``{`` //Sequence// ``}``
  |  |  **|**  | ``atomic`` ``{`` //Sequence// ``}``
  |  |  **|**  | ``d_step`` ``{`` //Sequence// ``}``
  |  |  **|**  | ``select`` ``(`` //Sequence// ``)``
  |  |  **|**  | ``{`` //Sequence// ``}``
  |  |  **|**  | //Send//
  |  |  **|**  | //Receive//
  |  |  **|**  | //Assign//
  |  |  **|**  | ``else``
  |  |  **|**  | ``break``
  |  |  **|**  | ``goto`` //Name//
  |  |  **|**  | //Name// ``:`` //Stmt//
  |  |  **|**  | //PrintType// ``(`` //Pargs// ``)``
  |  |  **|**  | ``assert`` //Expr//
  |  |  **|**  | //Name// ``(`` //ArgList// ``)``
  |  |  **|**  | //Expr//
  | //Range// | -> | //Name// ``in`` //Name//
  |  |  **|**  | //Name// ``:`` //AnyExpr// ``..`` //AnyExpr//
  | //Options// | -> | ``::`` //Sequence//
  |  |  **|**  | ``::`` //Sequence// //Options//
  | //RunPrio// | -> | **eps**
  |  |  **|**  | //Priority//
  | //RunArgs// | -> | **eps**
  |  |  **|**  | //ArgList//
  | //Expr// | -> | //AnyExpr//
  |  |  **|**  | ``(`` //Expr// ``)``
  |  |  **|**  | //Expr// //AndOr// //Expr//
  |  |  **|**  | //ChanPoll// ``(`` //VarRef// ``)``
  | //Uname// | -> | //Name//
  | //Name// | -> | //PIdent//



%% File generated by the BNF Converter (bnfc 2.9.4).
