-- File generated by the BNF Converter (bnfc 2.9.4).

{-# LANGUAGE CPP #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE LambdaCase #-}
#if __GLASGOW_HASKELL__ <= 708
{-# LANGUAGE OverlappingInstances #-}
#endif

-- | Pretty-printer for Obs.

module Obs.Print where

import Prelude
  ( ($), (.)
  , Bool(..), (==), (<)
  , Int, Integer, Double, (+), (-), (*)
  , String, (++)
  , ShowS, showChar, showString
  , all, elem, foldr, id, map, null, replicate, shows, span
  )
import Data.Char ( Char, isSpace )
import qualified Obs.Abs

-- | The top-level printing method.

printTree :: Print a => a -> String
printTree = render . prt 0

type Doc = [ShowS] -> [ShowS]

doc :: ShowS -> Doc
doc = (:)

render :: Doc -> String
render d = rend 0 False (map ($ "") $ d []) ""
  where
  rend
    :: Int        -- ^ Indentation level.
    -> Bool       -- ^ Pending indentation to be output before next character?
    -> [String]
    -> ShowS
  rend i p = \case
      "["      :ts -> char '[' . rend i False ts
      "("      :ts -> char '(' . rend i False ts
      "{"      :ts -> onNewLine i     p . showChar   '{'  . new (i+1) ts
      "}" : ";":ts -> onNewLine (i-1) p . showString "};" . new (i-1) ts
      "}"      :ts -> onNewLine (i-1) p . showChar   '}'  . new (i-1) ts
      [";"]        -> char ';'
      ";"      :ts -> char ';' . new i ts
      t  : ts@(s:_) | closingOrPunctuation s
                   -> pending . showString t . rend i False ts
      t        :ts -> pending . space t      . rend i False ts
      []           -> id
    where
    -- Output character after pending indentation.
    char :: Char -> ShowS
    char c = pending . showChar c

    -- Output pending indentation.
    pending :: ShowS
    pending = if p then indent i else id

  -- Indentation (spaces) for given indentation level.
  indent :: Int -> ShowS
  indent i = replicateS (2*i) (showChar ' ')

  -- Continue rendering in new line with new indentation.
  new :: Int -> [String] -> ShowS
  new j ts = showChar '\n' . rend j True ts

  -- Make sure we are on a fresh line.
  onNewLine :: Int -> Bool -> ShowS
  onNewLine i p = (if p then id else showChar '\n') . indent i

  -- Separate given string from following text by a space (if needed).
  space :: String -> ShowS
  space t s =
    case (all isSpace t', null spc, null rest) of
      (True , _   , True ) -> []              -- remove trailing space
      (False, _   , True ) -> t'              -- remove trailing space
      (False, True, False) -> t' ++ ' ' : s   -- add space if none
      _                    -> t' ++ s
    where
      t'          = showString t []
      (spc, rest) = span isSpace s

  closingOrPunctuation :: String -> Bool
  closingOrPunctuation [c] = c `elem` closerOrPunct
  closingOrPunctuation _   = False

  closerOrPunct :: String
  closerOrPunct = ")],;"

parenth :: Doc -> Doc
parenth ss = doc (showChar '(') . ss . doc (showChar ')')

concatS :: [ShowS] -> ShowS
concatS = foldr (.) id

concatD :: [Doc] -> Doc
concatD = foldr (.) id

replicateS :: Int -> ShowS -> ShowS
replicateS n f = concatS (replicate n f)

-- | The printer class does the job.

class Print a where
  prt :: Int -> a -> Doc

instance {-# OVERLAPPABLE #-} Print a => Print [a] where
  prt i = concatD . map (prt i)

instance Print Char where
  prt _ c = doc (showChar '\'' . mkEsc '\'' c . showChar '\'')

instance Print String where
  prt _ = printString

printString :: String -> Doc
printString s = doc (showChar '"' . concatS (map (mkEsc '"') s) . showChar '"')

mkEsc :: Char -> Char -> ShowS
mkEsc q = \case
  s | s == q -> showChar '\\' . showChar s
  '\\' -> showString "\\\\"
  '\n' -> showString "\\n"
  '\t' -> showString "\\t"
  s -> showChar s

prPrec :: Int -> Int -> Doc -> Doc
prPrec i j = if j < i then parenth else id

instance Print Integer where
  prt _ x = doc (shows x)

instance Print Double where
  prt _ x = doc (shows x)

instance Print Obs.Abs.ObsString where
  prt _ (Obs.Abs.ObsString i) = doc $ showString i
instance Print Obs.Abs.Int where
  prt _ (Obs.Abs.Int i) = doc $ showString i
instance Print Obs.Abs.TInt where
  prt _ (Obs.Abs.TInt i) = doc $ showString i
instance Print [Obs.Abs.Obs] where
  prt _ [] = concatD []
  prt _ (x:xs) = concatD [prt 0 x, prt 0 xs]

instance Print Obs.Abs.Name where
  prt i = \case
    Obs.Abs.Mname obsstring -> prPrec i 0 (concatD [prt 0 obsstring])

instance Print Obs.Abs.Args where
  prt i = \case
    Obs.Abs.ArgsOne obsstring -> prPrec i 0 (concatD [prt 0 obsstring])
    Obs.Abs.ArgsCons obsstring args -> prPrec i 0 (concatD [prt 0 obsstring, prt 0 args])

instance Print Obs.Abs.LMsg where
  prt i = \case
    Obs.Abs.LmsgOne obsstring -> prPrec i 0 (concatD [prt 0 obsstring])
    Obs.Abs.LmsgCons obsstring lmsg -> prPrec i 0 (concatD [prt 0 obsstring, prt 0 lmsg])

instance Print Obs.Abs.Taskname where
  prt i = \case
    Obs.Abs.TaskName obsstring -> prPrec i 0 (concatD [prt 0 obsstring])

instance Print Obs.Abs.Varname where
  prt i = \case
    Obs.Abs.VarName obsstring -> prPrec i 0 (concatD [prt 0 obsstring])

instance Print Obs.Abs.Varval where
  prt i = \case
    Obs.Abs.VarVal obsstring -> prPrec i 0 (concatD [prt 0 obsstring])

instance Print Obs.Abs.Typename where
  prt i = \case
    Obs.Abs.TypeName obsstring -> prPrec i 0 (concatD [prt 0 obsstring])

instance Print Obs.Abs.State where
  prt i = \case
    Obs.Abs.State obsstring -> prPrec i 0 (concatD [prt 0 obsstring])

instance Print Obs.Abs.Varindex where
  prt i = \case
    Obs.Abs.VarIndex n -> prPrec i 0 (concatD [prt 0 n])

instance Print Obs.Abs.Obs where
  prt i = \case
    Obs.Abs.ObsName name -> prPrec i 0 (concatD [doc (showString "@@@"), doc (showString "0"), doc (showString "NAME"), prt 0 name])
    Obs.Abs.ObsLog int lmsg -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "LOG"), prt 0 lmsg])
    Obs.Abs.ObsInit int -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "INIT")])
    Obs.Abs.ObsTask int taskname -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "TASK"), prt 0 taskname])
    Obs.Abs.ObsSignal int n -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "SIGNAL"), prt 0 n])
    Obs.Abs.ObsDef int varname varval -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "DEF"), prt 0 varname, prt 0 varval])
    Obs.Abs.ObsDeclVal int typename varname varval -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "DECL"), prt 0 typename, prt 0 varname, prt 0 varval])
    Obs.Abs.ObsDecl int typename varname -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "DECL"), prt 0 typename, prt 0 varname])
    Obs.Abs.ObsDeclArr int typename varname varval -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "DCLARRAY"), prt 0 typename, prt 0 varname, prt 0 varval])
    Obs.Abs.ObsCall int args -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "CALL"), prt 0 args])
    Obs.Abs.ObsState int n state -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "STATE"), prt 0 n, prt 0 state])
    Obs.Abs.ObsStruct int varname -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "STRUCT"), prt 0 varname])
    Obs.Abs.ObsSeq int varname scalar -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "SEQ"), prt 0 varname, prt 0 scalar])
    Obs.Abs.ObsPtr int varname varval -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "PTR"), prt 0 varname, prt 0 varval])
    Obs.Abs.ObsScalar int varname varval -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "SCALAR"), prt 0 varname, prt 0 varval])
    Obs.Abs.ObsScalarIndex int varname varindex varval -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "SCALAR"), prt 0 varname, prt 0 varindex, prt 0 varval])
    Obs.Abs.ObsEnd int varname -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 int, doc (showString "END"), prt 0 varname])

instance Print Obs.Abs.Scalar where
  prt i = \case
    Obs.Abs.ObsScalarCons n varval scalar -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 n, doc (showString "_"), prt 0 varval, prt 0 scalar])
    Obs.Abs.ObsScalarNone end -> prPrec i 0 (concatD [prt 0 end])

instance Print Obs.Abs.End where
  prt i = \case
    Obs.Abs.ObsEndSeq n varname -> prPrec i 0 (concatD [doc (showString "@@@"), prt 0 n, doc (showString "END"), prt 0 varname])
